# prereport acks. An ack is a claim that someone looked; it is NOT a fix.
# Format: shape<TAB>file<TAB>text   (# lines carry the reason)
# Regenerated 2026-09-12 against `git diff 4e22204` (phase 3, all review rounds).
#
# These keys are the exact added-line TEXT, so rewrapping a line silently
# un-acks it and it comes back as a live finding. That is the intended
# failure direction -- an edited line deserves a fresh look -- but it means
# a stale ack is invisible. Regenerate rather than hand-editing.

# the _Static_assert IS the check: NW_ARR_TYPE(nw_unit, brick, uint8_t, ...) makes a retype to char a build error, and a build error is a test that fails
mechanism-claim	blob.h	* function. The type assert is what stops it quietly becoming a string
# controlled: widening _is_hex64 to accept a clean absolute path turns path-traversal-refused red. Run, quoted in the commit
mechanism-claim	tests/run.py	# BOTH SHAPES, and the CLEAN one is the load-bearing case. A traversing
# false positive: prose in a docstring, not an assertion
unpaired-absence	bakery/nw-cc.py	checked here as well as in nwcheck.c because the baker is not in the TCB
# prose, guards, and two PAIRED absences: expect('brick hash' not in out) is followed by expect('open brick image' in out), and expect('house=NAME' not in out) by expect(out.count('open brick image') >= 2) -- in both the positive is the evidence the code under test ran
unpaired-absence	tests/run.py	if plen is None:
# prose, guards, and two PAIRED absences: expect('brick hash' not in out) is followed by expect('open brick image' in out), and expect('house=NAME' not in out) by expect(out.count('open brick image') >= 2) -- in both the positive is the evidence the code under test ran
unpaired-absence	tests/run.py	THE IMAGE LANDS ON THE MACHINE ROOT, not in the stage, and that is
# prose, guards, and two PAIRED absences: expect('brick hash' not in out) is followed by expect('open brick image' in out), and expect('house=NAME' not in out) by expect(out.count('open brick image') >= 2) -- in both the positive is the evidence the code under test ran
unpaired-absence	tests/run.py	expect("brick hash" not in out,
# prose, guards, and two PAIRED absences: expect('brick hash' not in out) is followed by expect('open brick image' in out), and expect('house=NAME' not in out) by expect(out.count('open brick image') >= 2) -- in both the positive is the evidence the code under test ran
unpaired-absence	tests/run.py	expect(f"house={name}" not in out,
# prose, guards, and two PAIRED absences: expect('brick hash' not in out) is followed by expect('open brick image' in out), and expect('house=NAME' not in out) by expect(out.count('open brick image') >= 2) -- in both the positive is the evidence the code under test ran
unpaired-absence	tests/run.py	f"{magic} IS NOT IN plan-formats.txt. A new magic has to be "
# not a count -- the claim is that the hash becomes text exactly once, which is the phase-3 argument, pinned by test_brick_hash_revalidated_at_the_supervisor
prose-count	nwspawn.c	* handoff is the one place the hash becomes text again, and
# same claim as nwspawn.c, same pin
prose-count	nwsup.c	* the phase-3 argument would be false in the one place it matters.
# describes the assertions immediately below it and the deliberate second copy; both visible at the point of reading, not a number a later commit falsifies silently
prose-count	proofs/caller_nw_check.c	* two assertions must say so rather than reading brick[0].
# describes the assertions immediately below it and the deliberate second copy; both visible at the point of reading, not a number a later commit falsifies silently
prose-count	proofs/caller_nw_check.c	/* OPEN-CODED ON PURPOSE, and this is the one place in the
# pre-existing text; the width list is derived from blob.h and the number describes what the code does
prose-count	proofs/leaf_path_ok.c	* whichever width it was called with: the one place a stub assumed more
# pre-existing text; the width list is derived from blob.h and the number describes what the code does
prose-count	proofs/leaf_path_ok.c	* hardcoding one entry, so a second width would come back on its own. */
# describes the loop directly beneath it, which generates exactly one case per byte -- the code is the assertion
prose-count	tests/run.py	# ONE ACCEPTANCE CASE PER BYTE POSITION, and it has to be ACCEPTANCE.
# describes the loop directly beneath it, which generates exactly one case per byte -- the code is the assertion
prose-count	tests/run.py	# ONE CASE PER BYTE POSITION, and that is not thoroughness for its
# false positive: matched 'phase 3 deleted that line', a phase number, not a count
prose-count	tools/coverage-tcb.sh	# the brick path check; phase 3 deleted that line, and tcb-review had
# false positive: prose inside a failure message ('is NOT in a brick'), not an assertion
unpaired-absence	tests/run.py	f"because the house is NOT in a brick at all, which is the same "
# false positive: describes the code on the next line, not a count that can age
prose-count	houses/layer.c	* WIDTH divides the logger's buffer, so every write is one whole line and
# false positive: describes the code on the next line, not a count that can age
prose-count	houses/layer.c	/* Reads at most n-1 bytes and NUL-terminates. Returns 0 when the file is
# false positive: describes the code on the next line, not a count that can age
prose-count	nwcheck.c	* the suite does not look at. Measured: two houses, one id, one file
# false positive: describes the code on the next line, not a count that can age
prose-count	tests/run.py	# loudly -- four tests red at once -- but every message was a size
# the three assertions beneath it ARE the check, and each is shown to matter by what satisfies the other two; they cannot be RUN here (no Landlock, the test skips by name) so this is a claim awaiting an environment rather than an unchecked one
mechanism-claim	tests/run.py	# load-bearing:
# false positive: describes the line beneath it, not a count that can age
unpaired-absence	HISTORY.md	- **`mknod` is not in the seccomp allow-list**, so the probe killed the
# false positive: describes the line beneath it, not a count that can age
unpaired-absence	HISTORY.md	- **`unlink` is not in it either**, which killed the house that has binds
# false positive: describes the line beneath it, not a count that can age
prose-count	houses/brick.c	* so every write is one whole line and a bounded read can only return
# false positive: describes the line beneath it, not a count that can age
prose-count	houses/brick.c	/* Built into one buffer and emitted as ONE padded line -- it was
# false positive: English prose about the file's own location, not an assertion
unpaired-absence	tools/landlock-assertions-dryrun.py	boot output. NOT A TEST, and deliberately not in tests/run.py.
# the comment's own next clause says nothing pins it; that is the honest form
mechanism-claim	houses/brick.c	* THE GATE IS LOAD-BEARING AND NOTHING PINS IT. `control` removed it
# false positive: English prose in a record, not an assertion
unpaired-absence	HISTORY.md	brief, not in the TCB, which is this repository's most common defect
# paired: the same test then requires trunc_root/trunc_bind to hold specific
# values, which only a probe that ran can produce. This rejects the fixture's
# own "I could not probe" answer so it cannot read as either outcome.
unpaired-absence	tests/run.py	expect(not v.startswith("unprobed"),
# set arithmetic in a tool, not a test assertion; a non-empty result exits 2
unpaired-absence	tools/landlock-assertions-dryrun.py	if n not in emits)
# false positive: "not one step" is English, and the steps below it are
# enumerated rather than counted -- adding a sixth ages nothing
prose-count	.claude/rules/harness.md	**The partial-gate trap.** `make test` is not one step. It stages, runs
# false positive: English prose reporting what a grep returned, not an assertion
unpaired-absence	CLAUDE.md	invariant 6; `claims` grepped and the string is not in this file
# false positive: prose naming a gap in prereport's own matcher; verified by
# reading COUNTED in tools/prereport.py, which has no `hit`
unpaired-absence	HISTORY.md	`make prereport` does not catch it — `hit` is not in its `COUNTED`
# false positive: a synthetic probe string quoted as command output, not a claim
prose-count	HISTORY.md	is_comment("- three files, see https://x/y"): True
# HISTORY.md is exempt as a record, and the heading names a closed dated episode
prose-count	HISTORY.md	## 64. Closing the prose work: four lines, and why stopping is a finding (2026-09-12)

# this IS the pairing: absent before, present after, asserted a few lines down
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{_layer_dir()}/{lid}"),
# paired by the staging assertions above it -- rc==0 and "slot B" in the output
unpaired-absence	tests/run.py	expect(not [d for d in os.listdir(f"{slots}/B") if d.startswith(".")],
# paired by expect("house=liveone" in out) on the line above: the boot ran and
# produced houses, so "candone absent" is about the candidate and not the boot
unpaired-absence	tests/run.py	expect("house=candone" not in out,
# the comment itself says it is argued and not tested, and control ran it:
# moving stage_layers after the renames leaves the suite green
mechanism-claim	tools/stage-candidate.py	# BEFORE THE RENAMES, and that order matters as much as the one
# a quotation of the retracted text, in a record, which is exempt
prose-count	HISTORY.md	*(This said "exits in exactly three places". There are five `_exit`
# paired where it is called: after the success it follows rc==0 and "slot B";
# after the baker, failed-validation and missing-nw-check refusals it follows
# that refusal's reason assertion. NOT after every refusal -- control found
# that overstatement, and found the one path that had no helper at all
unpaired-absence	tests/run.py	expect(not left, f"{why}: scratch left in the candidate slot: {left}")
# paired by expect("house=liveone" in out) above, and control measured that
# the tag IS present when the candidate boots, so it can fail for its reason
unpaired-absence	tests/run.py	expect("[candone]" not in out,
# this is the pairing's own precondition: absent now, so a later assertion
# about it means something
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{_layer_dir()}/l-cand-live"),
# paired by the positive: the same id IS created under the --root prefix
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{_layer_dir()}/{rlid}"),
# this IS the retraction: the sentence says the dot is NOT load-bearing for
# bootability, and control measured a non-dotted leftover behaving identically
mechanism-claim	tools/stage-candidate.py	# dot load-bearing for bootability, and dropping it left the suite
# HISTORY.md is exempt as a record; both head closed, dated sections
prose-count	HISTORY.md	## 67. The fix answered its own question everywhere but one place (2026-09-12)
prose-count	HISTORY.md	### Two assertions that could not fail
# preconditions for the assertions that follow them: absent now, so a later
# statement about presence or absence means something
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{_layer_dir()}/{olid}"),
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{_layer_dir()}/{clash_lid}"),
# paired by the positive two lines up: the candidate DID land in <slots>/B
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{pfx}{slots}"),
# inside _stage(), which runs it after every invocation -- the invocation
# itself is the positive, and the wrapper is what makes it unforgettable
unpaired-absence	tests/run.py	expect(not left, f"{why}: scratch left in slot {d}: {left}")
# _no_new_layers is only ever called with an id the case asserted absent
# beforehand, so the pairing is the precondition at each call site
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{_layer_dir()}/{i}"),
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{_layer_dir()}/{symlid}"),
# paired by the refusal above it: the tool ran, refused, and must have
# removed the slot directory it created for the attempt
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{slots}/FRESH"),
# paired by the rc!=0 in the same expression: the tool ran and refused, so
# "no traceback" is about how it refused rather than about it not running
unpaired-absence	tests/run.py	expect(r.returncode != 0 and "Traceback" not in (r.out + r.err),
# paired by the refusal assertion two lines down, and by the fixture baking a
# plan that names this layer -- without both, the absence proves nothing
unpaired-absence	tests/run.py	expect(not os.path.exists(f"{pfx}{_layer_dir()}/{slid}"),

# --- 2026-09-12, the fold (bakery/fold.py, bakery/test_fold.py) ---
# The oracle's three comparisons ARE the check: it walks the kernel's own
# overlay view and the folded image and requires them equal. The paired
# positive is the `built` dict at the end of the same check, which names
# each property and reports which one is missing -- so an all-empty
# census cannot satisfy the three, and a fixture that quietly stops
# building one says which. An earlier version of this ack enumerated
# what that block asserts and got two of the items wrong (a symlink was
# not checked to point at a directory, a path was not checked to be an
# empty directory); `claims` read the code. Enumerate the probe is the
# rule this serves, so the enumeration now lives in the code as keys.
# Controls: every mechanism named, each mutated and each seen red.
unpaired-absence	bakery/test_fold.py	expect(not missing, f"the fold lost {missing}; the house saw them")
unpaired-absence	bakery/test_fold.py	expect(not extra, f"the fold invented {extra}")
unpaired-absence	bakery/test_fold.py	expect(not differ, f"folded image disagrees with the kernel: {differ}")
# Skip guards, not assertions: they choose whether the check runs and name
# what goes untested when it does not. There is nothing to pair.
unpaired-absence	bakery/test_fold.py	if "overlay" not in open("/proc/filesystems").read():
unpaired-absence	bakery/test_fold.py	if err is None:
# `_erofs_ok` asks BOTH questions -- the tool AND /proc/filesystems, on
# the next line. The matcher sees only the first. This is the very
# distinction harness.md draws (detect the capability, not a tool that
# implies it) and the code already draws it.
tool-presence	bakery/test_fold.py	if not shutil.which("mkfs.erofs"): return False, "mkfs.erofs is not installed"
# English, not counts: "One entry" names what the function handles, and
# "step-4" is a reference to a numbered step in the same function.
prose-count	bakery/fold.py	"""One entry, type-aware. `where` names the tree the entry came from,
prose-count	bakery/fold.py	# Applied last, in the order recorded, so a step-4 file's mode wins

# --- 2026-09-13, the fold's second review round ---
# `control` settled these five by mutation, not by reading: each absence
# is paired by a positive in the same check that fails when the mechanism
# does not run. 59 is redundant beside an exact-content positive; 181 is a
# skip guard inside _erofs_ok, which asks BOTH questions; 240 asserts the
# CONTROL was applied and is paired by hashes either side of it; 296 sits
# beside `raised is not None`; 369 beside returncode==1 and the reason
# string -- covering THAT input, not the property, since two other inputs
# printed tracebacks until this round fixed them.
unpaired-absence	bakery/test_fold.py	expect(not os.path.lexists(p), "the whiteout itself was copied in")
unpaired-absence	bakery/test_fold.py	if "erofs" not in open("/proc/filesystems").read():
unpaired-absence	bakery/test_fold.py	expect("-T" not in no_T, "the control did not remove -T")
unpaired-absence	bakery/test_fold.py	expect("does-not-exist" not in raised,
unpaired-absence	bakery/test_fold.py	expect("Traceback" not in r.stderr, f"crashed: {r.stderr[:200]!r}")
# 149 was `control`'s one FIX rather than ack: its positive came from step
# 3 creating the directory, not from step 1 copying anything, so it passed
# with step 1 neutralised. A lower file outside etc is asserted present on
# the next line and step1-does-nothing now turns it red.
unpaired-absence	bakery/test_fold.py	expect(ls(p) == [], f"etc holds {ls(p)}, expected empty")
# Paired further down the same check by the same probe on a filesystem
# that CAN hold the marker, so "refuses" is not satisfied by refusing
# everything. (This said "two lines down" and it is six; a position is a
# count with an extra way to be wrong. `claims`.)
unpaired-absence	bakery/test_fold.py	expect(not can, f"the probe accepted a filesystem with no xattrs: "
# Not a test: a guard inside the mode pass. `mode is None` is how a
# symlink cancels a mode recorded for the same path.
unpaired-absence	bakery/fold.py	if mode is None or not os.path.exists(path) or os.path.islink(path):
# Prose in a dated record and in a docstring, quoting the wording being
# retracted. Not counts and not assertions.
prose-count	HISTORY.md	*This said "so there is one entry", which is wrong and wrong in the
prose-count	HISTORY.md	**"There is no --force" was false, nine lines from `--no-opaque-check`.**
mechanism-claim	HISTORY.md	**The qualifier is load-bearing and was missing.** Identity is a
unpaired-absence	HISTORY.md	not in stderr`, for a different input.
unpaired-absence	bakery/test_fold.py	were not in it -- a claim of exhaustiveness standing beside the very
# English, not counts -- except the fold.py one, which IS a count and is
# permitted because the three are named in the two sentences above it.
# The earlier reason here said "English" for all three, which is where
# the next reader learns which shapes pass, so it mattered. `claims`.
prose-count	Makefile	# running, one line of output scrolling past. Both controls that were run
prose-count	bakery/fold.py	# all three, each one input away from the fixture.
prose-count	bakery/test_fold.py	# left `make test` green at one check fewer, and the roster size is
# Paired by the two assertions directly above it, which prove the probe
# RAN and refused for the kernel's own errno. Without them "left nothing
# behind" would be satisfied by a probe that never created anything.
unpaired-absence	bakery/test_fold.py	expect(os.listdir(m) == [], f"the probe left {os.listdir(m)} behind")

# --- 2026-09-13, the fold's third review round ---
# English and step references, not counts.
prose-count	HISTORY.md	**Round two's crash moved one step over rather than being fixed.** Step 3
prose-count	Makefile	# green again, two echoed command lines with nothing between them, which is
prose-count	bakery/test_fold.py	# the house copies `etc` up, so step 1 and step 4a each supply it
unpaired-absence	HISTORY.md	not in the fold, because in the fold it would mean disagreeing with what
# The escape check's absences are paired by the block after them, which
# requires the merged tree to be RIGHT -- so "wrote nothing outside" cannot
# be satisfied by a fold that did nothing. 1012/1016 are the empty-dir case
# and pair each other: not-a-symlink AND is-an-empty-directory AND the host
# untouched, three readings of one outcome, so no single wrong result
# satisfies them.
unpaired-absence	bakery/test_fold.py	expect(not os.path.islink(os.path.join(t.merged, "x")),
unpaired-absence	bakery/test_fold.py	expect(not os.path.islink(m),
unpaired-absence	bakery/test_fold.py	expect(os.path.isdir(m) and ls(m) == [],
# Paired by the next line, which requires the symlink's TARGET to survive
# -- so "the whiteout removed it" is not satisfied by a merge that removed
# everything.
unpaired-absence	bakery/test_fold.py	expect(not os.path.lexists(os.path.join(t.merged, "bin")),
# Paired by the very next line, which requires an ORDINARY user.* xattr to
# survive -- so "the marker was not carried" cannot be satisfied by a fold
# that carried no xattrs at all, which is the mutation the round before
# this one was about.
unpaired-absence	bakery/test_fold.py	expect("user.overlay.somethingelse" not in got,
# Prose in a record, not an assertion in a test.
unpaired-absence	HISTORY.md	different failures and are not in tension, so the answer to "a review is

# --- 2026-09-13, the fold helper ---
# The mechanism-claim IS the finding: `control` measured that removing a
# pairing left the suite green, and the broken-scan case beside this
# comment is what makes the pairing load-bearing. Both pairings are red
# under `drop-environ-pairing` and `drop-mountinfo-pairing`.
mechanism-claim	tests/run.py	#     only input that makes either pairing load-bearing. Removing a
# English: "one house per line" is the output format, not a quantity.
prose-count	tools/fold-house.py	"""One house per line, in the syntax `load_city` reads.
# A skip guard naming what goes untested, not an assertion.
unpaired-absence	tests/run.py	if "overlay" not in open("/proc/filesystems").read():
# THE paired positive itself, inverted: this asserts the grandchild does
# NOT carry NW_LAYER, which is the precondition making the mountinfo case
# test what it exists for rather than duplicating the environ case. Its
# pair is the refusal below it that must name upperdir -- naming the
# assertion rather than counting lines, since the distance is a hostage.
unpaired-absence	tests/run.py	expect(fh.scan_environ(lid) == [],
# The pairing's own assertion: the scan must find a child exec'd WITH the
# variable. Absence here means the scan is broken, which is the whole
# point -- there is no positive to add, because this IS the positive for
# every later empty result.
unpaired-absence	tools/fold-house.py	if p.pid not in seen:
# Not a test: a lookup miss, reported with the names that were available.
unpaired-absence	tools/fold-house.py	if target is None:
# An ORDINAL naming the sidecar field, not a count of anything. "field 1"
# is the position the reader indexes; there is nothing here to grow.
prose-count	tests/run.py	# field 1 every clash case above would pass for the wrong reason.
# A parser guard, not an assertion: it decides whether a mountinfo line
# holds an upperdir= option at all. The paired positive is the marker
# overlay `_paired_mountinfo_probe` mounts, which the scan must find
# before any empty result is believed. Named rather than located: the
# first version of this ack gave a distance and got it wrong, in the
# same batch as an ack saying a distance is a hostage.
unpaired-absence	tools/fold-house.py	if " - overlay " not in line and ",upperdir=" not in line:
# Paired, and the pairing was added because prereport asked. Case 4
# takes _slot_state() either side of a SUCCESSFUL fold and requires the
# brick dir and the candidate slot to appear in the difference, so a
# _slot_state that could not see a change turns the test red THERE --
# after the refusals above it have passed vacuously, not before them.
# The first version of this ack had that ordering backwards and
# contradicted the code comment it was acking. `claims`.
unpaired-absence	tests/run.py	expect(not changed,
# Not an assertion: the guard that stops the machine-root check REPLACING
# the body's own failure. On a clean run it asserts; with an exception in
# flight it prints and lets the real failure stand. `control` produced the
# masking case that this exists to prevent.
unpaired-absence	tests/run.py	if leaked and sys.exc_info()[0] is None:
# Same guard, on the pairing above it. The paired positive IS this line:
# it requires _ours to claim every id the body made, so an empty `leaked`
# cannot be produced by a filter that matches nothing. prereport asked for
# this pairing and it was added; the hit is the guard, not the absence.
unpaired-absence	tests/run.py	if sys.exc_info()[0] is None:
# Quoting `make prereport`'s own output inside the record of the round it
# fired in. Not an assertion.
unpaired-absence	HISTORY.md	`make prereport` asked the question (`asserts an absence: 'is None'`)
# Paired by the line above it, which requires the refusal to NAME move_lid.
# Alone, "does not name keep_lid" is satisfied by a tool that refuses
# nothing; together they mean the guard is keyed per id.
unpaired-absence	tests/run.py	expect(keep_lid not in (r.out + r.err),
# A precondition, not a claim about the mechanism: it establishes that this
# id has NOT been staged in this process, so the half below is testing the
# first sighting rather than a later one. Without it the test could silently
# become a second copy of the second half.
unpaired-absence	tests/run.py	expect(lid not in _LAYERS_RESET,
# Paired by the `expect(os.path.exists(sentinel), "the sentinel was not
# planted, so its later absence would prove nothing")` that precedes it.
# The test plants the state itself precisely so this absence means the
# reset fired rather than that nothing was ever there.
unpaired-absence	tests/run.py	expect(not os.path.exists(sentinel),
# English, not a count of a list: "folding two different units in one test"
# names a thing that does not exist. Nothing here can grow past it.
prose-count	tests/run.py	# one test, which is not built.
# "§73" matched as a count. A section name, not a quantity.
prose-count	tests/run.py	#    matched either and `HISTORY.md` §73 recorded this case as red
# Paired by the `expect(on_disk, ...)` immediately above it, which
# requires the body to have created layers under its own root. Without
# that, an empty listing makes "nothing was missed" true for the wrong
# reason.
unpaired-absence	tests/run.py	expect(not missed,
# Quotes the count it is condemning. HISTORY is exempt as a record, and
# the sentence exists to say the number should not have been written.
prose-count	HISTORY.md	**`Twenty-two controls` in that message is an unnamed instance-count.**
# Names both instances in the two sentences beneath it (the flat cp, and
# the anchor written from a report). A named list, which the rule permits.
prose-count	HISTORY.md	**Two controls in the same script, two different ways of testing
# A sentinel, not an assertion: `lids = None` distinguishes "the key was
# not given" from `lids=none`, which parses to 0. The paired positive is
# in test_baker_rejects -- omitting the key is refused AND lids=none is
# accepted, so a baker refusing both would fail the second half.
unpaired-absence	bakery/nw-cc.py	if lids is None:
# A sentinel on the --probe path, the twin of the city-file one above:
# argparse has no default now, so None means the flag was not given.
# Paired in test_baker_rejects -- `--probe` with no `--lids` refused,
# `--lids none` accepted. The pair is what makes it a claim: a baker
# refusing both would satisfy the refusal half alone.
unpaired-absence	bakery/nw-cc.py	if args.lids is None:
# Paired directly below by `expect(os.path.exists(probe-none.blob))`:
# the same tool on the same flag must WRITE its --out when it accepts,
# so "absent after a refusal" is about the refusal and not about a tool
# that writes nothing. `control` moved the bake above the check and the
# refusal left a validated blob behind.
unpaired-absence	tests/run.py	expect(not os.path.exists(probe_no),
# Paired by the seccomp bake below it, which requires _lids_byte to read
# NONZERO. Every other lids assertion here wants 0, so a reader that
# always answered 0 would satisfy them all -- the instrument has to be
# shown seeing a difference before its zeroes mean anything.
unpaired-absence	tests/run.py	expect(not bad,

# --- 2026-09-13, the resource block. Each looked at; none is a fix. ---
# a dated record of a closed episode: three fields is the number that WAS wrong, and the fix names all three
prose-count	HISTORY.md	**One code for three fields, under a comment promising one per field.**
# names the control's exact input on the next line; a measured one-line mutation, not a count of instances
prose-count	HISTORY.md	the one line `/* e.g. NW_TYPE(nw_unit, kind, uint8_t); pins the kind
# not a count: a requirement that the define stay on one line, enforced by unit_layout()'s single-line regex, which goes red if it does not
prose-count	blob.h	/* ONE LINE, deliberately: tests/run.py derives the unit size by matching
# not a count: a rule about how the codes are allocated. Being wrong here is visible in the enum three lines down
prose-count	nwcheck.c	* ONE CODE PER FIELD, because a refusal naming "the resource
# a property of blob.h's declaration style, checkable by looking at any NW_AT line, and the reason the pattern is un-anchored
prose-count	plan-formats.txt	# `^\s*`, and blob.h writes two declarations per line -- `NW_AT(nw_unit,
# describes a control that was run: one comment line added, nothing else changed. The input, not a tally
prose-count	plan-formats.txt	# so one added comment line moved the signature and the failure told the
# names both cases in the sentence before it (nice=-1@other and nice=1@other), which is the form CLAUDE.md permits
prose-count	tests/run.py	# filename, so two distinct cases share one artifact. Harmless
# same property of blob.h's style as the plan-formats.txt ack above
prose-count	tests/run.py	# line's history. blob.h writes two declarations per line --
# same property of blob.h's style as the plan-formats.txt ack above
prose-count	tests/run.py	# two declarations per line and anchoring hid the second one on
# a membership test inside a refusal, not an absence assertion: the branch it guards raises, and test_baker_refuses_bad_resources asserts the reason string for sched=fifo
unpaired-absence	bakery/nw-cc.py	if v not in SCHED_NAMES:
# paired in the same test, immediately above: the line is first asserted PRESENT and to name exactly ["bare0", "bare1"], so the mechanism is shown running before it is shown silent
unpaired-absence	tests/run.py	expect("no resource block" not in (p.out + p.err),
# paired in the same test (the mutated run was green for THIS test only -- path-traversal-refused was red): the refusal cases assert the exit code AND the reason string first (so the baker ran and refused for the stated reason), and the acceptance loop asserts the file EXISTS after a legal bake -- without that half, "no blob written" would be satisfied by a baker that never writes one. Both halves added in the same round, after `control` moved check() below the write for a green run
unpaired-absence	tests/run.py	expect(not os.path.exists(out),

# --- 2026-09-20, Saturday queue items 1/2/4/5. Each looked at; none is a fix. ---
# meta-commentary about a CLAUSE in the comment above it, not a claim that code has a mechanism -- and the paragraph's whole point is that "if any" is NOT a safety argument
mechanism-claim	blob.h	/* "if any" is the load-bearing clause and it is not a safety argument.
# names both cases in the two bullets immediately below (no brick; layer shadow), which is the redundant-but-checkable form CLAUDE.md permits
prose-count	blob.h	* Two cases where resolution is NOT inside a sealed image:
# not an instance count: "the only one with no line" is a statement about which death printed nothing, and the `spent` line this patch adds is what makes it false going forward
prose-count	nwsup.c	* death that ENDED the house was the only one with no line at all,
# "the two" are the two lists named in the sentence before it -- this loop and initrd-init.c's load() calls; naming, not counting, and the paragraph exists because they disagreed
prose-count	tools/mkboot.sh	# a load-order bug, changing nothing. Keep the two in step as

# Item 3 (the fd pre-flight). 2026-09-20.
# blob.h: the 4 is a VALUE -- the setting of NW_FD_RESERVED whose
# control is quoted -- not a count of anything. The matcher read
# '4 and the check' as four checks.
prose-count	blob.h	* Set it to 4 and the check ACCEPTS a city that then dies at
# blob.h: 'the one place' is English, not an instance-count; the
# claim is about WHERE the 6 lives, and it names the place.
prose-count	blob.h	* the counted 6 in the compiler, which is the one place "counted peak is
# pid1.c: '2 pointer file' is the return code -2 being documented.
# Same false positive the fd-auditor and tcb-review rounds both
# identified independently.
prose-count	pid1.c	/* 0 ok, -2 pointer file absent (unchosen), -1 present but untrusted. */
# HISTORY.md: prose about an arithmetic identity, not a test
# assertion -- there is nothing to pair it with. The claim is
# checked by reading 6 + n, which contains no 8.
unpaired-absence	HISTORY.md	8; the counted `6 + n` does not contain it at all, which is
# tests/run.py, all three: paired, and the docstring says so and
# says which half is the pin. The positives are rc == 70, 'plan
# sealed', 'HALT: fd ' and the three numbers; the absences only
# rule out falling through to pipe2 discovery. Controls run at
# n=3 against three mutants: pre-flight deleted -> red, soft-raise
# deleted -> red, -1 init deleted -> STILL GREEN and recorded in
# the docstring as an uncovered gap rather than acked away.
unpaired-absence	tests/run.py	expect("HALT: log pipe" not in out,
unpaired-absence	tests/run.py	expect("city open" not in out,
unpaired-absence	tests/run.py	expect("HALT: fd " not in out2, f"accepted plan still refused\n{out2}")

# tests/run.py, the interleave test's halt census. Paired on both sides
# in the same test: `plan sealed` before it (the run got as far as the
# pipes) and `city open houses=n` plus `closed houses_reaped=n` after
# it (the houses ran and were reaped). An empty halt list cannot mean
# "never booted" with those three present. Controlled: the sequential
# ordering makes this the assertion that fails, naming HALT: log pipe.
unpaired-absence	tests/run.py	expect(not halts, f"halted at rlimit {need}: {halts}\n{out}")

# .claude/rules/plan.md, the gate re-filing paragraph. "not in a
# paragraph attached to a format bump" is English, not an absence
# assertion -- there is no test and nothing to pair. The matcher's
# known false-positive class on prose files.
unpaired-absence	.claude/rules/plan.md	change, not in a paragraph attached to a format bump.

# docs/relayed/*, twenty entries below. These are documents committed
# UNCHANGED, byte-identical to what was relayed, and editing one to
# satisfy a heuristic would destroy the record item 8 exists to keep.
# The counts prereport finds in them are other people's prose about
# other trees; the absences are their own third-line status labels
# ("Not in the tree"), which is the labelling CLAUDE.md asks for, read
# by a matcher built for test assertions. Not fixable here and not
# defects. An ack is a claim that someone looked, and I read each hit.
#
# The shape worth noticing rather than acking: prereport reported CLEAN
# on this diff before these files were committed and 21 after, because
# untracked files are not in the diff it scans. So item 8's own
# pre-commit gate result was about a tree state that was not the one
# being committed -- the same family as HISTORY 83 and 84, in the tool
# bought to catch that family. Recorded here because it is the only
# place a future reader of these acks will look.
prose-count	docs/relayed/NW-BOOT-RECORD.md	**`reason` and `err` are two fields, not one.** The first draft merged
prose-count	docs/relayed/NW-FROM-SCRATCH-RECORD.md	### 4.1 The mechanism rule
prose-count	docs/relayed/NW-FROM-SCRATCH-RECORD.md	**Why clause 4 earns its place.** An audit proposed exempting *numbers
prose-count	docs/relayed/NW-SOLVENT.md	### PID 1 becomes fifty lines
prose-count	docs/relayed/NW-VM-AS-HOUSE-RECORD.md	**Capabilities, not paths.** Already argued in `blob.h` for one field: the
prose-count	docs/relayed/NW-VM-AS-HOUSE-RECORD.md	*cannot express* a traversal. `nwcheck.c:109` names the two fields that
unpaired-absence	.claude/rules/plan.md	"Written 2026-09-14. Not in the tree", and the directory it sits in
unpaired-absence	docs/relayed/NW-BAKE-LEDGER.md	Written 2026-09-14. Not in the tree. Entirely offline: no PID 1, no
unpaired-absence	docs/relayed/NW-BAKE-LEDGER.md	One append-only file in the bakery, not on the machine and not in the
unpaired-absence	docs/relayed/NW-BAKE-LEDGER.md	A refusal whose `detail` names a unit the source does not contain. Two
unpaired-absence	docs/relayed/NW-BOOT-RECORD.md	Written 2026-09-14. Not in the tree.
unpaired-absence	docs/relayed/NW-BOOT-RECORD.md	over a bounded record must report **not in the retained volume** rather
unpaired-absence	docs/relayed/NW-BOOT-RECORD.md	that change is not in the tree, so nobody has run the count against a tree
unpaired-absence	docs/relayed/NW-BOOT-RECORD.md	oldest `boot` in the ring is not 1, and must render that as **not in the
unpaired-absence	docs/relayed/NW-FIRST-FIRMWARE-BOOT.md	Measured 2026-09-14. Not in the tree when written. `NEXUSWEAVE.md` §11 lists
unpaired-absence	docs/relayed/NW-FROM-SCRATCH-RECORD.md	Two of the most expensive errors were not in any design. A specification
unpaired-absence	docs/relayed/NW-FROM-SCRATCH-RECORD.md	than format properties, stale reads reported as *not in the retained
unpaired-absence	docs/relayed/NW-MEASUREMENTS-0914.md	Not in the tree. Every number below was produced by running something on
unpaired-absence	docs/relayed/NW-UNCONSTRAINED-ADDENDUM-7.md	does not contain `lid_brick`. Same privilege, less code. Smaller than the
unpaired-absence	docs/relayed/NW-UNCONSTRAINED-ADDENDUM-7.md	`brickcatalogue.py` is not in the worktree and has no object anywhere in
unpaired-absence	docs/relayed/NW-VIRTIO-STATUS.md	Measured 2026-09-14. Not in the tree when written. Answers the one item

# .claude/rules/plan.md: the line QUOTES NW-BOOT-RECORD.md's own status
# line in order to say the document is not scheduled. A quotation of a
# label, not an assertion about the tree.
unpaired-absence	.claude/rules/plan.md	"Written 2026-09-14. Not in the tree", and the directory it sits in

# .claude/rules/runtime.md, the rescue section. Two English matches.
# "no handler is installed" is about a signal handler in pid1.c, not a
# tool on the machine -- the matcher's `is installed` looking for the
# mkfs.vfat shape and finding a sentence about sigaction. And "is NOT
# in that chain" is a statement of territory membership, not a test
# assertion; there is nothing to pair it with, and the positive half
# ("which is yours") is the same sentence.
tool-presence	.claude/rules/runtime.md	signals are not blocked at that point and no handler is installed, so
unpaired-absence	.claude/rules/runtime.md	machine. **Plus `rescue.c`, which is yours and is NOT in that chain** —

# The ownership census and the cwd delivery test, 2026-09-21.

# Paired by the assertion on the line above it, which requires the stamp to
# be present under the REPO's .reviews. Together they say the stamp went to
# one specific place; either alone is satisfied by a hook that writes no
# stamp at all, which is the silent-no-op this pair exists to catch.
unpaired-absence	tests/run.py	expect(not os.path.exists(os.path.join(foreign, ".reviews")),


# Not a test assertion -- this is --check's own classification branch, the
# line that decides a file is unaccounted for. The matcher is looking for
# an unpaired absence in a test and found an `is None` in a tool.
unpaired-absence	tools/rules-hook.sh	elif not t and why is None:

# The finding is the discrepancy, so the number IS the claim and cannot
# age: the fixture came up short by one against whatever the tree held
# that day. The absolute counts that would age were removed from this
# comment in the same edit.
prose-count	.gitignore	# built that way came up one code file short.

# Re-keyed: the unowned probe moved from tools/mkboot.sh to
# tools/review-gate.sh when mkboot.sh was given to runtime, so the old
# ack's text no longer matches. Same pairing as before -- the delivery
# assertions above it fire the hook successfully from the same foreign
# cwd under the same settings.json command, so a hook that had gone
# silent fails there first.
unpaired-absence	tests/run.py	expect(fire(foreign, os.path.join(ROOT, unowned)) is None,

# The multi-owner round, 2026-09-21. English in every case: "two rules
# files", "the two cases", "two sets of rules" and "ten files" are the
# claims themselves, not tallies of anything that can grow. "one this
# case wants" is the matcher reading the word one out of a sentence.
prose-count	tests/run.py	# TWO OWNERS GET TWO RULES FILES, in a stable order. Leaving such a
prose-count	tools/rules-hook.sh	# --check refuses it, naming both. The two cases are indistinguishable
prose-count	tools/rules-hook.sh	# its rules should arrive, and a file governed by two sets of rules
prose-count	tools/rules-hook.sh	# Once per rules file until that file changes, so touching ten files in one

# Three --check branches, not test assertions. The matcher is looking
# for an unpaired absence in a test and finding the classification and
# validation logic of the tool.
#
# The reason here used to say the census test exercised the refusals
# these produce. It did not -- it planted only unaccounted files, so
# each of these branches was deletable green, and "a control turns it
# red" meant planting the DATA, which shows the mechanism works rather
# than pinning it. `control` called that an ack asserting a pairing
# that does not exist, and it was right. The test now plants a
# condition per refusal and requires each by its own message, so
# removing any of them turns it red; the ack stands on that.
unpaired-absence	tools/rules-hook.sh	if dec is None:
unpaired-absence	tools/rules-hook.sh	unknown = [x for x in terrs if x not in territories()]

# Re-keyed after the sentence above it was rewritten: same false
# positive, the matcher reading the word "one" out of "the one this
# case wants".
prose-count	tests/run.py	# that looks like the one this case wants.

# The rooting round, 2026-09-21. Every entry below is either English or a
# negative paired inside its own test; listed one by one because an ack
# saying "all of these are fine" is an instance-count wearing a reason.

# "the one case that must never happen quietly" -- English, and the
# sentence is the claim rather than a tally.
prose-count	tools/rules-hook.sh	# tree's rules is worse than either -- so the one case that must never

# The matcher hunts the mkfs-vfat shape, "tool X is installed therefore
# feature X works". This is the hook's own diagnostic telling a reader
# it was installed outside the repository it serves.
tool-presence	tools/rules-hook.sh	f"{os.path.realpath(self)}). The hook is installed outside the "

# Six negatives in the two hook tests, each paired inside its own test:
#   - the Bash unowned case, paired by the Bash positive just above it
#     that delivers runtime for a command naming nwsup.c;
#   - the stub's "no rules dir", a fixture assertion whose positive is
#     the symlink case at the end of the same test, which shows the hook
#     delivering when it CAN root;
#   - "code files" absent from the stub's refusal, paired with the
#     assertions that it refused and named the path it tried;
#   - "## Liveness" absent from the stub's diagnostic, paired with the
#     assertion that the diagnostic is present and names the path;
#   - no stamp in the real repo after a misinstalled call, paired by the
#     stamp assertions in the delivery test;
#   - "enumerated" absent from the census refusal, paired with the two
#     assertions naming the planted files.
unpaired-absence	tests/run.py	expect(not (q.stdout or b"").decode().strip(),
unpaired-absence	tests/run.py	expect(not os.path.isdir(os.path.join(t, ".claude", "rules")),
unpaired-absence	tests/run.py	expect("code files" not in (c.out + c.err),
unpaired-absence	tests/run.py	expect("## Liveness" not in ctx,
unpaired-absence	tests/run.py	expect(not os.path.isfile(os.path.join(ROOT, ".reviews",
unpaired-absence	tests/run.py	expect("enumerated" not in (q.out + q.err),

# --check's own refusal text, not a test assertion. The stub case in
# test_rules_hook_refuses_a_tree_it_cannot_root_in turns it red: a hook
# copied where there is no .claude/rules must refuse and name the path
# it tried.
unpaired-absence	tools/rules-hook.sh	print(f"rules-hook --check: not in a checkout -- no {rules_dir} "

# The stale-exemption branch in --check, not a test assertion. The gate
# case "an UNOWNED row for a file that never existed" in
# test_every_code_file_is_accounted_for turns it red, by its own message
# ("is not a tracked code file").
unpaired-absence	tools/rules-hook.sh	if p not in tracked:

# The final round, 2026-09-21.

# "make test is not one step" -- English, and the sentence immediately
# after it now says to read the target rather than giving a number.
prose-count	.claude/rules/harness.md	**The partial-gate trap.** `make test` is not one step. **Read the target

# The reverse MAP loop's own branch -- a declared name matching no
# tracked file -- not a test assertion. The gate case "a MAP name for a
# file that does not exist" turns it red, by its own message ("no
# tracked code file is called that").
unpaired-absence	tools/rules-hook.sh	if b not in bases:

# The follow-up note about unpaired-absence firing on non-test code,
# flagged by unpaired-absence, on the line quoting the tokens it fires
# on. Twenty-fifth row of the round and the clearest single argument for
# the narrowing the note describes.
unpaired-absence	CLAUDE.md	— `--check`'s classification and validation branches, whose `is None` and

# The sweep round, 2026-09-21.

# The hook's and the test's wrong-checkout diagnostic, both the matcher
# reading English -- "installed in or linked from a different checkout"
# is where the hook IS, not a claim that some tool exists.
tool-presence	tests/run.py	f"is installed in or linked from a different checkout, and its "
tool-presence	tools/rules-hook.sh	f"its own location. It is installed in, or linked from, a "

# Absences, each paired inside its own case: the wrong-tree
# diagnostic's "## Liveness" is paired with the assertion that the
# refusal is present and names both trees; the gate's "OK" absence is
# paired with the assertions that it refused and gave the census
# reason; the anchor line is a fixture guard whose whole job is to
# abort when a plant target moved.
unpaired-absence	tests/run.py	expect("## Liveness" not in d,
unpaired-absence	tests/run.py	expect("install-agents: OK" not in (g.out + g.err),
unpaired-absence	tests/run.py	f"{label}: the anchor {o!r} is not in the hook exactly "

# The triage round, 2026-09-21.

# Two in the gate-refusal table's own scaffolding, not assertions about
# the tree: `held is None` is the restore helper deciding whether the
# file existed before the plant, and the "not in the copy" line is a
# fixture guard that aborts when a plant target is absent. Both are the
# machinery that makes the planted cases mean something.
unpaired-absence	tests/run.py	if held is None:
unpaired-absence	tests/run.py	f"{label}: {rel} is not in the copy, so removing "

# The hand-pin round, 2026-09-21. Every entry below is English, a
# fixture guard, or a negative paired inside its own case -- and each
# of the pinned cases was shown red under the mutation it exists for
# before this file was touched.

# The heredoc loop's `[ -e "$f" ] || continue`. The sentence names a
# mechanism and the case directly beneath it is the check: with the
# line deleted, a missing brief is also reported as diverged and the
# "must be absent" field turns it red. Run.
mechanism-claim	tests/run.py	# in the heredoc loop is what stops a brief that is not there

# `head -1` is a command's flag, not a tally -- the matcher read "1
# check" out of the name of the check being discussed.
prose-count	tests/run.py	# satisfies all of them; deleting the head -1 check left the

# English: the sentence quotes the tokens prereport itself fires on,
# which is a hit for its own vocabulary, in the paragraph measuring
# that vocabulary.
unpaired-absence	CLAUDE.md	`--check`'s classification and validation branches, whose `is None` and

# The malformed-event case, paired twice over: the two assertions
# immediately above it require exit 0 and empty stdout on the SAME
# event, and the well-formed deliveries earlier in the test are the
# positive that the hook runs at all. stderr is the assertion here
# because the shell wrapper exits 0 on the event path regardless --
# deleting the try/except leaves status and stdout right and turns
# only this one red. Run.
unpaired-absence	tests/run.py	expect(b"Traceback" not in (q.stderr or b""),

# A fixture guard, not a claim about the tree: it aborts if the
# rooted-but-not-a-checkout fixture somehow has a .git, which would
# make the refusal below about something else.
unpaired-absence	tests/run.py	expect(not os.path.exists(os.path.join(nogit, ".git")),

# Paired on the line above it, which requires the wanted message. The
# pair IS the assertion: a refusal naming the right fault and a wrong
# one beside it satisfies `want in output` on its own, which is how
# the missing-brief case stayed green with the heredoc loop's guard
# deleted.
unpaired-absence	tests/run.py	expect(unwanted not in (r.out + r.err),
